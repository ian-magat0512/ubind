export enum SectionDisplayMode {
    Article = "article", // display one article at a time within a step
    ArticleElement = "articleElement", // display one article element (e.g. one question set) at a time
    Page = "page" // display everything, e.g. all articles and all question sets on the page at once.
}
