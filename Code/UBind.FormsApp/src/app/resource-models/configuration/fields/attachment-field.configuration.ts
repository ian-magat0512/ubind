import { InteractiveFieldConfiguration } from "./interactive-field.configuration";

/**
 * Represents a field which allows a file to be uploaded and attached
 */
export interface AttachmentFieldConfiguration extends InteractiveFieldConfiguration {
}
