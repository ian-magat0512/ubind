import { OptionsFieldConfiguration } from "./options-field.configuration";

/**
 * Represents the configuration for a field which renders options as a set of buttons.
 */
export interface ButtonsFieldConfiguration extends OptionsFieldConfiguration {
}
