import { ToggleFieldConfiguration } from "./toggle-field.configuration";

/**
 * Represents the configuration for a field which can be toggled on or off.
 */
export interface CheckboxFieldConfiguration extends ToggleFieldConfiguration {
}
