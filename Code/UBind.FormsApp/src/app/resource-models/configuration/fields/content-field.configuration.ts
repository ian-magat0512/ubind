import { VisibleFieldConfiguration } from "./visible-field.configuration";

/**
 * Represents the configuration for a field that only displays content and does not have any interaction with the user.
 */
export interface ContentFieldConfiguration extends VisibleFieldConfiguration {
}
