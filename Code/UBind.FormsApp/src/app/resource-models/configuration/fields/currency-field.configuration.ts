import { SingleLineTextFieldConfiguration } from "./single-line-text-field.configuration";

/**
 * Represents the configuration for a field which allows someone to input a currency value.
 */
export interface CurrencyFieldConfiguration extends SingleLineTextFieldConfiguration {
}
