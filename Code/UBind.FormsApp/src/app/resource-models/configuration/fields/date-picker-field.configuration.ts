import { SingleLineTextFieldConfiguration } from "./single-line-text-field.configuration";

/**
 * Represents the configuration for a field which stores a date and provides a date picker to help the user in
 * selecting a date.
 */
export interface DatePickerFieldConfiguration extends SingleLineTextFieldConfiguration {
}
