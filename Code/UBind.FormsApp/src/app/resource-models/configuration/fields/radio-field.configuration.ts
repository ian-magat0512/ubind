import { OptionsFieldConfiguration } from "./options-field.configuration";

/**
 * Represents the configuration for a field which renders the options sa radio buttons.
 */
export interface RadioFieldConfiguration extends OptionsFieldConfiguration {
}
