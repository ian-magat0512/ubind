/**
 * Represents the icons to display as a symbol in the progress step. Applies in order of precedence.
 */
export interface ProgressStepIcons {
    active: string;
    first: string;
    last: string;
    future: string;
    past: string;
    inactive: string;
}
