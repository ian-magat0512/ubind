/**
 * The resource model returned after sending a successful iqumulate response to the server.
 */
export interface IQumlateFundingResponseResourceModel {
    quoteId: string;
    pageResponse: any;
    calculationResultId: string;
}
