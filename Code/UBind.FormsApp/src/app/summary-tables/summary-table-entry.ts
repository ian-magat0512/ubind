import { Htmlifyable } from "./htmlifyable";

/**
 * Represents an entry in a Summary Table, so that we can have different types of entries that can be 
 * converted to html.
 */
export interface SummaryTableEntry extends Htmlifyable {
}
