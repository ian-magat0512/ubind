/**
 * enum for representing SamplePeriodLength types.
 */
export enum ChartSamplePeriodLength {
    Day = 'day',
    Month = 'month',
    Quarter = 'quarter',
    Year = 'year',
    All = 'all',
    Custom = 'custom',
}
