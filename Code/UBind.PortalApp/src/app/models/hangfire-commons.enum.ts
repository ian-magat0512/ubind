/** Represents hangfire related flag and value
 * used for redirection to the hangfire dashboard.
 */
export enum HangfireCommons {
    DashboardLocationFlag = 'redirectLocation',
    DashboardLocationValue = 'hangfire',
    RedirectionFlag = 'isRedirected',
}
