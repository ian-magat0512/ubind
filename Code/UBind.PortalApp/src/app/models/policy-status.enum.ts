export enum PolicyStatus {
    Active = 'Active',
    Issued = 'Issued',
    Expired = 'Expired',
    Cancelled = 'Cancelled',
    Adjusted = 'Adjusted',
    Renewed = 'Renewed',
    Current = 'Current',
}

export enum CustomerPolicyStatus {
    Current = 'Current',
    Active = 'Active',
    Inactive = 'Inactive',
}
