/**
 * Model for Product release settings that contins the configuration pero quote type.
 */
export interface ProductReleaseSettingsModel {
    quoteType: string;
    doesUseDefaultProductRelease: boolean;
}
