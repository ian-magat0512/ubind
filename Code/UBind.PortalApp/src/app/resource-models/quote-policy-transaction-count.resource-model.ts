/**
 * The interface for quote and policy transaction counts resource model.
 */
export interface QuotePolicyTransactionCountResourceModel {
    quoteCount: number;
    policyTransactionCount: number;
}
